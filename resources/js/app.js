//  import "./bootstrap";
import Highcharts from 'highcharts';

import { Modal, Ripple, Button, Input, Select, Dropdown, Collapse, Popconfirm, initTE } from "tw-elements";
initTE({ Modal, Ripple, Button, Input, Select, Dropdown, Popconfirm, Collapse }, true);

 